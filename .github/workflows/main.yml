name: main

on:
  push:
    branches:
      - deploy

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install lftp
        run: sudo apt install lftp
      - name: Deploy app build to paceenet
        run: cd build && lftp -c "set ftp:list-options -a;
              open ftp:// ${{ secrets.PTF_CAKE }}:${{ secrets.PTF_SAUCE }}@ftp.pacee.net;
              cd /react;
              mirror --reverse --delete --use-cache --verbose -i ."
